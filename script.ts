import * as rls from 'readline-sync';

registrarUsuario ();
navegacion ();
realizarCompra ();
favoritos();

function registrarUsuario () {
    let nombre: String;
    let ciudad: String;
    let edad: number;
    let año: number;
    let mes: number;
    let dia: number;
    let metodoDePago: number;

    //se pide al usuario que ingrese sus datos y se guardan en las variables

    
}

function navegacion () {
    //El sitio tiene una barra de navegación para buscar un mueble por categoría.
}

function realizarCompra () {
    //El usuario puede guardar en una lista los muebles que quiere comprar. 
    //El usuario tiene múltiples opciones de pago. 

    //lista numerica de opciones de pago y un if-else if

    //llame a una funcion que le pida al usuario con que va a pagar que le pasa por parametro con que va a pagar
    if(vaAPagarCon=="tarjeta"){
        //pedir si va a pagar con debito o credito
        if(debito){
            //pedir los datos de la tarjeta
        }
        else if(credito){
            //pedir los datos de la tarjeta
            //y en cuantas cuotas lo va a hacer
        }
    }
    else if(transferencia){
        //mostrar por pantalla el CBU o alias
    }
}
function favoritos(){
    //if usuario === like
    //añadir al arreglo
}


